[project]
name = "walkie"
version = "0.1.0"
description = "라즈베리파이 두 대로 만드는 와이파이 무전기 (PTT 하프 듀플렉스)"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9",
    "numpy>=1.24",
    "sounddevice>=0.4.6",
]
